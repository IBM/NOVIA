language: python
dist: bionic
addons:
  apt:
    packages:
      - cmake
      - g++-7
python:
  - "3.6" # default 
install:
  - ./compile_llvm.sh
  - cd fusion
  - mkdir build
  - cd build
  - source ../../env.sh
  - cmake ../
  - make
